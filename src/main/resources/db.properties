# Default database configuration. Values can be overridden via
# system properties (e.g. -Ddb.url=...), environment variables
# (DB_URL, DB_USER, DB_PASS, DB_POOL_MAX, DB_POOL_MIN_IDLE) or by
# placing an overriding file at ~/.budget/db.properties.
db.url=jdbc:mysql://127.0.0.1:3306/budget?useSSL=false&serverTimezone=UTC
db.user=root
db.password=
db.pool.maxSize=10
db.pool.minIdle=2
